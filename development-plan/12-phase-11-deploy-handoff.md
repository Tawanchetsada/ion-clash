# Phase 11 · Deploy และส่งมอบ

> **เป้าหมาย:** เว็บออนไลน์ใช้งานได้จริง มีเอกสารครบ และพร้อมใช้กับนิสิต 8 คนในวันทดลอง
> **ต้องรอ:** Phase 10

---

## 11.1 Deploy

- [ ] deploy จาก `main` ขึ้น production บน Vercel
- [ ] ตั้ง `NEXT_PUBLIC_RESEARCH_ENDPOINT` ใน Vercel dashboard **เท่านั้น** ห้ามอยู่ในซอร์ส
- [ ] ตรวจว่า production build ใช้ endpoint ถูกตัว
- [ ] ทดสอบ URL production บน iPad จริงอีกรอบ ไม่ใช่แค่ preview
- [ ] ตั้ง Content Security Policy
- [ ] ยืนยันว่าเปิดครั้งแรกแล้วโหลดเสร็จ เล่นต่อได้แม้เน็ตหลุดกลางทาง

---

## 11.2 README

ต้องตอบให้ครบตาม AC-15

| หัวข้อ | เนื้อหา |
|---|---|
| โปรเจกต์นี้คืออะไร | สรุปสั้น พร้อมลิงก์ production และ repo |
| ติดตั้งและรัน | `npm ci` แล้ว `npm run dev` |
| คำสั่งทั้งหมด | dev build start lint typecheck test test:e2e |
| รันเทสต์ไฟล์เดียว | `npx vitest run <path>` และ `npx vitest run -t "<ชื่อ>"` |
| **วิธีแก้ข้อมูลด่าน** | แก้ที่ `src/data/levelSeeds.ts` เท่านั้น แล้วรัน `npm test` |
| **วิธีเพิ่มไอออนใหม่** | เพิ่มใน `ions.ts` แล้วเพิ่มกฎใน `solubility.ts` |
| โครงสร้างโฟลเดอร์ | อธิบายชั้น domain / data / components / storage / research |
| ระบบวิจัย | ลิงก์ไป `docs/research-setup.md` |
| deploy | เชื่อม Vercel กับ repo แล้ว push |

---

## 11.3 เอกสารส่งมอบ

- [ ] `docs/chemistry-review.md` ที่อาจารย์เซ็นรับรองแล้ว ครบ 50 ด่าน
- [ ] `docs/research-setup.md` คู่มือติดตั้ง Google Apps Script ทีละขั้น
- [ ] `docs/assumptions.md` รายการสมมติฐานที่ตั้งไว้ระหว่างพัฒนา
- [ ] `docs/not-done.md` รายการสิ่งที่ยังไม่ได้ทำ ต้องไม่มีข้อใดเป็น MUST
- [ ] อัปเดต `CLAUDE.md` ให้ตรงกับโค้ดจริง เพราะตอนนี้เขียนไว้ตอนยังไม่มีโค้ด

**สิ่งที่ต้องระบุใน `assumptions.md` อย่างน้อย**

- ตัด O2- MnO4 2- และ CN- ออกจากสำรับการ์ดเดิม พร้อมเหตุผล (D-02)
- เพิ่ม Pb2+ Ba2+ I- Br- S2- ที่ไม่มีในสำรับการ์ด
- ตีความ "ไอออน 4 ถึง 4" เป็นการจับคู่ใหม่โดยใช้สัมประสิทธิ์บอกจำนวน (D-03)
- สารที่ละลายน้อยซึ่งจัดเป็นตะกอน ได้แก่ PbCl2 PbSO4 MgCO3
- เว็บกับสำรับการ์ดกายภาพไม่ตรงกัน 100%

---

## 11.4 คู่มือวันทดลอง

เอกสารหน้าเดียวสำหรับผู้วิจัยใช้จริงหน้างาน `docs/pilot-runbook.md`

**เตรียมก่อนวัน**

- [ ] ทดสอบ Apps Script ยิงข้อมูลเข้า Sheet ได้จริง อย่างน้อย 1 สัปดาห์ก่อน
- [ ] เตรียม iPad หรือเครื่องให้ครบ 8 เครื่อง เปิด URL ค้างไว้
- [ ] เคลียร์ localStorage ของทุกเครื่องก่อนเริ่ม
- [ ] พิมพ์บัตรรหัสผู้เรียน S01 ถึง S08
- [ ] เตรียมแบบทดสอบหลังเรียนแบบกระดาษ
- [ ] เตรียมลิงก์ Google Form แบบประเมินความพึงพอใจ
- [ ] **ซ้อมเต็มรูปแบบ 1 รอบ** ด้วยเครื่องจริงและบัญชีจริง

**วันทดลอง**

1. แจกบัตรรหัส ให้นักเรียนกรอกรหัสในเว็บ
2. อธิบายและให้ความยินยอมการเก็บข้อมูล
3. ให้เล่นตามเวลาที่กำหนด
4. **ก่อนเลิก ให้ทุกคนกดปุ่มคัดลอกผลการเรียนแล้ววางลง Google Form** ข้อนี้สำคัญที่สุด อย่าให้ใครลืม
5. ทำแบบทดสอบหลังเรียนบนกระดาษ
6. ทำแบบประเมินความพึงพอใจใน Google Form

**หลังทดลอง**

1. ตรวจว่า Google Sheet มีข้อมูลครบ 8 คน
2. ถ้าขาด ให้ใช้ข้อมูลจาก Google Form ที่นักเรียนวางไว้แทน
3. เปิดหน้า `/research` นำเข้าข้อมูล กรอกคะแนน E2 จากกระดาษ
4. ส่งออก CSV สำหรับใส่ในเล่มวิทยานิพนธ์

> ข้อ 4 ของวันทดลองคือจุดที่พลาดง่ายที่สุดและกู้คืนยากที่สุด ควรมีคนคอยไล่เช็คทีละเครื่อง

---

## 11.5 ตรวจครั้งสุดท้าย

- [ ] `npm run lint` `npm run typecheck` `npm test` `npm run build` ผ่านครบ
- [ ] E2E ผ่านครบทั้ง 3 project
- [ ] ข้อมูล 50 ด่านผ่าน validation
- [ ] อาจารย์เซ็นรับรองเนื้อหาเคมีแล้ว
- [ ] ทดสอบ iPad เครื่องจริงแล้ว
- [ ] ไม่มี console error
- [ ] save schema มี version migration test export import และ reset
- [ ] README และเอกสารส่งมอบครบ
- [ ] ซ้อมวันทดลองแล้ว 1 รอบ

---

## กับดักที่ต้องระวัง

| กับดัก | ผลที่ตามมา |
|---|---|
| ทดสอบแค่ preview deployment | production ใช้ environment variable คนละตัว อาจไม่ส่งข้อมูลวิจัย |
| ไม่ซ้อมก่อนวันจริง | เจอปัญหาหน้างานแล้วแก้ไม่ทัน เสียโอกาสเก็บข้อมูลจากนิสิต 8 คน |
| ลืมเคลียร์ localStorage ก่อนเริ่ม | ข้อมูลรอบทดสอบปนกับข้อมูลจริง |
| ไม่ไล่เช็คว่านักเรียนกดคัดลอกครบทุกคน | ข้อมูลบางคนหาย กู้ไม่ได้ |
| อัปเดต CLAUDE.md ไม่ทัน | คนที่มาทำต่อได้ข้อมูลผิดตั้งแต่บรรทัดแรก |
